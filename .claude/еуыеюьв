backend                Redis                Orchestrator       Embedding      Moderation    Similarity    Classifier
   │  BuildTripMLPayload  │                       │                 │              │             │            │
   │ ───XADD tasks───────▶│                       │                 │              │             │            │
   │                      │ ─XREADGROUP──────────▶│                 │              │             │            │
   │                      │                       │ download media (parallel httpx)│             │            │
   │                      │                       │ ───embed bundle───────▶│      │             │            │
   │                      │                       │ ◀──img/text embed, phash──────┤             │            │
   │                      │                       │ ───moderate(imgs, texts)────────▶          │            │
   │                      │                       │ ◀──nsfw_ids, nsfw_text_ids─────────────────┤            │
   │                      │                       │ ───similarity(per pin)──────────────────▶ │            │
   │                      │                       │ ◀──similar_groups─────────────────────────┤            │
   │                      │                       │ ───classify(per new pin)──────────────────────▶        │
   │                      │                       │ ◀──{category, tags}───────────────────────────────────┤
   │                      │                       │ async XADD pin-vector → Qdrant                          │
   │                      │ ◀──XADD results───────│                                                          │
   │ ◀─XREADGROUP results │                       │                                                          │