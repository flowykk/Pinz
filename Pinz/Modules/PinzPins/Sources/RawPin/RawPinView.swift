import SwiftUI
import PinzDomain

public struct RawPinView: View {

    private let pin: RawPin

    public init(pin: RawPin) {
        self.pin = pin
    }

    public var body: some View {
        
    }
}
