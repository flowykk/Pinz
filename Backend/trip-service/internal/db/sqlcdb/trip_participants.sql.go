// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: trip_participants.sql

package sqlcdb

import (
	"context"

	"github.com/google/uuid"
)

const tripParticipantAdd = `-- name: TripParticipantAdd :exec
INSERT INTO trip_participants (trip_id, user_id, is_admin) VALUES ($1, $2, $3)
`

type TripParticipantAddParams struct {
	TripID  uuid.UUID
	UserID  uuid.UUID
	IsAdmin bool
}

func (q *Queries) TripParticipantAdd(ctx context.Context, arg TripParticipantAddParams) error {
	_, err := q.db.ExecContext(ctx, tripParticipantAdd, arg.TripID, arg.UserID, arg.IsAdmin)
	return err
}

const tripParticipantClearAdmin = `-- name: TripParticipantClearAdmin :exec
UPDATE trip_participants SET is_admin = false WHERE trip_id = $1
`

func (q *Queries) TripParticipantClearAdmin(ctx context.Context, tripID uuid.UUID) error {
	_, err := q.db.ExecContext(ctx, tripParticipantClearAdmin, tripID)
	return err
}

const tripParticipantIsAdmin = `-- name: TripParticipantIsAdmin :one
SELECT is_admin FROM trip_participants WHERE trip_id = $1 AND user_id = $2
`

type TripParticipantIsAdminParams struct {
	TripID uuid.UUID
	UserID uuid.UUID
}

func (q *Queries) TripParticipantIsAdmin(ctx context.Context, arg TripParticipantIsAdminParams) (bool, error) {
	row := q.db.QueryRowContext(ctx, tripParticipantIsAdmin, arg.TripID, arg.UserID)
	var is_admin bool
	err := row.Scan(&is_admin)
	return is_admin, err
}

const tripParticipantIsParticipant = `-- name: TripParticipantIsParticipant :one
SELECT 1 AS ok FROM trip_participants WHERE trip_id = $1 AND user_id = $2 LIMIT 1
`

type TripParticipantIsParticipantParams struct {
	TripID uuid.UUID
	UserID uuid.UUID
}

func (q *Queries) TripParticipantIsParticipant(ctx context.Context, arg TripParticipantIsParticipantParams) (int32, error) {
	row := q.db.QueryRowContext(ctx, tripParticipantIsParticipant, arg.TripID, arg.UserID)
	var ok int32
	err := row.Scan(&ok)
	return ok, err
}

const tripParticipantListByTrip = `-- name: TripParticipantListByTrip :many
SELECT trip_id, user_id, is_admin, joined_at FROM trip_participants WHERE trip_id = $1
`

func (q *Queries) TripParticipantListByTrip(ctx context.Context, tripID uuid.UUID) ([]TripParticipant, error) {
	rows, err := q.db.QueryContext(ctx, tripParticipantListByTrip, tripID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TripParticipant{}
	for rows.Next() {
		var i TripParticipant
		if err := rows.Scan(
			&i.TripID,
			&i.UserID,
			&i.IsAdmin,
			&i.JoinedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const tripParticipantRemove = `-- name: TripParticipantRemove :execrows
DELETE FROM trip_participants WHERE trip_id = $1 AND user_id = $2
`

type TripParticipantRemoveParams struct {
	TripID uuid.UUID
	UserID uuid.UUID
}

func (q *Queries) TripParticipantRemove(ctx context.Context, arg TripParticipantRemoveParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, tripParticipantRemove, arg.TripID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const tripParticipantRemoveAllByTrip = `-- name: TripParticipantRemoveAllByTrip :exec
DELETE FROM trip_participants WHERE trip_id = $1
`

func (q *Queries) TripParticipantRemoveAllByTrip(ctx context.Context, tripID uuid.UUID) error {
	_, err := q.db.ExecContext(ctx, tripParticipantRemoveAllByTrip, tripID)
	return err
}

const tripParticipantSetAdmin = `-- name: TripParticipantSetAdmin :exec
UPDATE trip_participants SET is_admin = true WHERE trip_id = $1 AND user_id = $2
`

type TripParticipantSetAdminParams struct {
	TripID uuid.UUID
	UserID uuid.UUID
}

func (q *Queries) TripParticipantSetAdmin(ctx context.Context, arg TripParticipantSetAdminParams) error {
	_, err := q.db.ExecContext(ctx, tripParticipantSetAdmin, arg.TripID, arg.UserID)
	return err
}
