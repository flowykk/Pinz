apiVersion: v1
kind: Service
metadata:
  name: trip-service
  labels:
    app: trip-service
spec:
  selector:
    app: trip-service
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: grpc
  type: ClusterIP
